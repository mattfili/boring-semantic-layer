Return aggregated domain context for this server: a list of available skills (Level 1 metadata only — name, description, file inventory, URI prefix) plus a brief summary of every model. Call this before any non-trivial analytical task to discover whether the server has skill-based guidance for the domain (e.g. tax-form interpretation, grant-network conventions, peer-benchmarking methodology) and which models are available. Skill bodies and additional files are NOT included in the response — fetch them via `read_resource("skill://<name>/SKILL.md")` only when you need the full instructions. Re-scans the configured skills_dir on every call, so newly added skills appear without a server restart.
